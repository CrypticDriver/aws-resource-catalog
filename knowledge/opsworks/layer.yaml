schema_version: v1
status: stub
service: opsworks
service_code_arn_namespace: opsworks

resource_type:
  name: layer
  human_name: AWS::OpsWorks::Layer
  cfn: AWS::OpsWorks::Layer
  config: AWS::OpsWorks::Layer
  resource_explorer: AWS::OpsWorks::Layer

billing:
  billable: true
  model: complex
  summary: |
    TODO — verify pricing model and CUR usage type from official docs.
  free_tier: false

lifecycle:
  parent_resource: null
  auto_delete_on_parent_delete: false

orphan_detection:
  orphan_prone: false

tags: [stub]

verification:
  contributor: auto-generated
  doc_url: http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-opsworks-layer.html
